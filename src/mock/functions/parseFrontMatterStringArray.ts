import { __UNIMPLEMENTED__ } from '#util';

export function parseFrontMatterStringArray(
	frontmatter: any | null,
	key: string | RegExp,
	nospaces?: boolean,
): string[] | null {
	__UNIMPLEMENTED__();
}
